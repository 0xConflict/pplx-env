[project]
name = "computer-skills"
version = "0.2.0"
description = "Pre-built skills for Perplexity Computer"
requires-python = ">=3.10"
license = "MIT"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
